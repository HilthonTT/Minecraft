namespace Minecraft.Core.Utilities.Spatial;

/// <summary>
/// The four corners of a block face, used when baking per vertex lighting into a chunk mesh.
/// </summary>
public enum Corner
{
    TopRight,
    TopLeft,
    BottomLeft,
    BottomRight,
}
